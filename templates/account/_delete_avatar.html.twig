<form method="post" action="{{ path('app_avatar_delete', {'id': user.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer votre avatar ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.image) }}">
    <button class="button button-outline button-small trs-trl-left">Supprimer ma photo</button>
</form>