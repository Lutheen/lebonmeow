{% extends "base.html.twig" %}

{% block title %}Profil de {{ app.user.username }}{% endblock %}

{% block body %}

<div class="container">
    <div class="main">
        <div class="user-card">
            {{ app.user.username }}
            Membre depuis {{ app.user.dateFormat }}
        </div>
        <div class="user-products-card">
            {{ app.user.soldProducts|length }} annonces
        </div>
    </div>
</div>

{% endblock %}